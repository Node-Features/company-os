// Package artifact is reserved for the canonical Artifact domain contract
// (docs/domain/artifact.md, Status: APPROVED) but has no implementation
// yet — no type in this package, and no equivalent exists anywhere else
// in this codebase (docs/audit/backlog-p2-p4.md's "doc-only stubs" row).
// The closest structural neighbor, internal/domain/result.Result, is an
// evidentiary event record, not an addressable, versioned, lifecycle-
// tracked Artifact — do not treat it as a substitute.
package artifact
