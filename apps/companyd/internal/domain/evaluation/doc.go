// Package evaluation is reserved for the canonical Evaluation domain
// contract (docs/domain/evaluation.md, Status: APPROVED) but has no
// implementation yet — no Evaluation, EvaluationDesign, Benchmark, or
// PerformanceProfile type exists here (docs/audit/backlog-p2-p4.md's
// "doc-only stubs" row). The real, narrower equivalent is
// internal/domain/monitoringevaluation.Evaluation and
// monitoringevaluation.PerformanceProfile — department-scoped, with a
// minimal field set the M&E department's own doc comment self-discloses
// as "narrower than [this contract's] full aspirational contract."
package evaluation
