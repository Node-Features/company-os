// Package evidence is reserved for the canonical Evidence domain contract
// (docs/domain/evidence.md, Status: APPROVED) but has no implementation
// yet — no type in this package (docs/audit/backlog-p2-p4.md's "doc-only
// stubs" row). The closest equivalent is internal/domain/research.Evidence,
// scoped to the Research department only (no generic, cross-department
// Evidence type exists).
package evidence
