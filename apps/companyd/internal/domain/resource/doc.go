// Package resource implements the canonical Resource domain contract:
// ResourceConstraint and its specializations (CostConstraint,
// ComputeConstraint, TimeConstraint, StorageConstraint,
// ConcurrencyConstraint). Pure types only — no I/O.
// See docs/domain/resource.md.
package resource
