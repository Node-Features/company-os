// Package workspace is reserved for the canonical Workspace domain
// contract (docs/domain/workspace.md, Status: APPROVED) but has no
// implementation yet — no Workspace or EngineeringWorkspace type exists
// here or anywhere else in this codebase
// (docs/audit/backlog-p2-p4.md's "doc-only stubs" row). ROADMAP.md Phase
// 6 Slice 1 (Workspace lifecycle) is the first slice that would build one.
package workspace
